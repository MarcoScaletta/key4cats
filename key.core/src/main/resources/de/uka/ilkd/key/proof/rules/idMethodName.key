\sorts{
    MethodName;
}
\predicates{
//    traceContractToBound{false,true,true,true,false}(MethodName,$Formula,$Formula,$Formula,int);
    assumeCATof(MethodName, $Formula);
    holdBindingCATof(MethodName, $Formula);
    CATof(MethodName, $Formula);
    contractApplication($Formula,$Formula);
    bind{true}($Formula);
    CAT($Formula,$Formula,$Formula);

    preTrace($Formula,$Formula);
    innerBehavior($Formula,$Formula);
    continue($Formula,$Formula,$Formula);
    observeForContract($Update, $Formula);
    observeForContractApplication($Formula, $Formula);
    readyForContractApplication($Formula, $Formula);
    observeForCall($Update, $Formula);
    applyContract($Update, $Formula);
}






//    unboundCATTraces{true,true,true,false}($Formula,$Formula,$Formula,int);